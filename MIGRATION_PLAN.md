# 草稿から Wiki への移行計画

## 目的

`drafts/` 以下の草稿を、単純な複製ではなく、理論的な内容、記法、日本語表現、内部リンク、タグを検証したうえで `docs/wiki/` へ移行する。

草稿は移行後も原典として保持する。

移行済みページは、教科書の章立てだけに依存せず、分野階層タグと横断概念タグの両方から探索できる状態にする。

## 基本方針

- 移行は分野ごとに進める。
- 一度のバッチは、依存関係が近い 3 本から 8 本に限る。
- 既存ページの記法、用語、リンク形式を優先する。
- 草稿の記述を正しいと仮定しない。定義の仮定、定理の結論、証明、例、出典が支持する範囲を確認する。
- 移行先は原則として `docs/wiki/<concept>.md` とする。
- 草稿内の旧形式のリンクは、移行先が存在し内容上も適切であることを確認してから更新する。
- 移行後に `uv run mkdocs build` を実行し、リンク、タグ索引、警告差分を確認する。

## 分野別ロードマップ

| フェーズ | 分野 | 主な対象 | 前提 |
| --- | --- | --- | --- |
| 0 | 共通基盤 | 濃度、関数、同型、写像の像 | なし |
| 1 | 抽象代数の基礎 | 半群、モノイド、群作用、巡回群、体、環準同型、イデアル | 既存の群、加法群、環 |
| 2 | 線形代数・加群論 | 線形写像、双線形写像、加群、テンソル積、自由加群、射影加群 | 環論 |
| 3 | 位相の基礎 | 位相空間、連続写像、近傍系、閉包作用素、コンパクト性、連結性 | 共通基盤 |
| 4 | 解析・関数解析 | 距離空間、測度空間、バナッハ空間、ヒルベルト空間、有界作用素、スペクトル定理 | 位相、線形代数 |
| 5 | 微分幾何 | 多様体、接ベクトル束、微分形式、リーマン計量、曲率 | 位相、線形代数 |
| 6 | ホモロジー代数 | 鎖複体、ホモロジー、完全列、Ext と Tor、導来関手 | 加群論 |
| 7 | 圏論の基礎 | 函手、自然変換、極限、余極限、随伴、米田の補題 | 既存の圏 |
| 8 | モノイダル・豊穣・高次圏論 | モノイダル圏、豊穣圏、双圏、準圏 | 圏論の基礎 |

## 進捗

| フェーズ | 状態 | 移行済みページ |
| --- | --- | --- |
| 0 | 完了 | 濃度、写像、像、逆射、同型射 |
| 1 | 進行中 | 巡回群、群作用、体、環準同型、イデアル、モノイド作用、モノイド準同型、部分モノイド、部分環 |

各フェーズは、前のフェーズの全ページを完了させる必要はない。

ただし、移行対象の定義が依存する概念は、先に移行するか、既存ページへ補足を加える。

## バッチごとの手順

1. 対象草稿を選び、概念間の依存関係、既存ページ、リンク先、未定義記号を棚卸しする。
2. 定義の仮定、定理・命題の結論、例の条件、証明の論理を検証する。
3. 日本語を常体に統一し、一文一行、段落ごとの一主題、客観的で根拠に対応した表現へ推敲する。
4. 記号、単位元、零元、演算記号、用語の表記を既存ページと統一する。
5. 移行先ページを作成または更新し、関連概念へのリンクを追加する。
6. YAML front matter にタグを付与する。
7. `uv run mkdocs build` を実行し、警告とリンクを確認する。
8. 変更対象と検証結果を記録して、バッチ単位でレビュー可能なコミットにする。

## タグ付け

各ページには、少なくとも一つの分野・階層タグと、一つから三つの横断概念タグを付ける。

- 分野・階層タグの例：`代数学/群論`、`代数学/環論`、`位相幾何学`、`圏論`
- 横断概念タグの例：`加法構造`、`準同型`、`商構成`、`部分構造`、`生成`、`可換性`、`分配法則`

一般的すぎるタグ（`数学`、`定義`、`命題`、`証明`）は付けない。

既存のタグ名を再利用し、新しいタグは、既存の語彙で関係を表せない場合に限って追加する。

## 初回バッチ

フェーズ 1 から、次の 5 概念を候補とする。

1. 巡回群
2. 群作用
3. 体
4. 環準同型
5. イデアル

このバッチは既存の群・環・加法群ページに接続しやすく、代数学の分野階層タグと横断概念タグの運用を検証できる。

巡回群は群の整数冪と群の位数を参照するため、これらの記法とリンクを最初に確認する。

体、環準同型、イデアルは環の単位元と可換性に関する約束を共有するため、同一バッチで整合させる。

## 完了条件

各ページは、次をすべて満たした時点で移行済みとする。

- 定義、仮定、記号が明示され、既存ページと矛盾しない。
- 本文の主張、例、証明が確認済みである。
- 日本語がプロジェクトの文体規範に従う。
- 分野・階層タグと横断概念タグが付いている。
- 関連ページへのリンクが解決する。
- `uv run mkdocs build` が成功し、新たな警告を導入していない。
