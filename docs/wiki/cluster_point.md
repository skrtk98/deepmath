---
tags:
  - 幾何学/位相幾何学
  - 閉包
  - 近傍
---

# 触点

部分集合の任意の近傍に現れる点を、その集合の**触点**という。

## 定義

> [!definition] 触点
> 位相空間 $`X`$ の部分集合 $`A`$ と点 $`x`$ に対し、任意の $`x`$ の近傍 $`U`$ が $`U\cap A\ne\varnothing`$ を満たすとき、$`x`$ を $`A`$ の触点という。

触点全体は $`A`$ の [閉包](./closure_operator.md) $`\overline{A}`$ に一致する。
